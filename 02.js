// importacao

const promp = require("prompt-sync")()
const not1 = Number(prompt("digite a nota1: "))
const not2 = Number(prompt("digite a nota2: "))
const med = (nota1 + nota2)/2

//processamento

if (media >=6){
    console.log("aprovado")
}
else {
    console.log("reprovado")

}