const prompt = require ("prompt-sync")()

const cateadj = Number (prompt("digite o valor do cateto adjasente: "))
const cateopst = Number (prompt("digite o valor do cateto oposto: "))

const resultado = hipo=(catopos **2 + cateadj **2)**0.5

console.log(resultado)






